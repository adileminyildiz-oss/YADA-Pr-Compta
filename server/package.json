{
  "name": "yada-supabase-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Couche serveur Supabase (handlers withSupabase) — SÉPARÉE du site statique GitHub Pages (precompta.html). Ne se déploie pas sur Pages : nécessite un runtime Node/Edge. La clé secrète reste UNIQUEMENT en variable d'environnement, jamais committée.",
  "scripts": {
    "dev": "node index.mjs"
  },
  "dependencies": {
    "@supabase/server": "^1.1.0"
  }
}
